 // Max Speed cap
if (object_ship_jason.speed >= PLAYER_MAX_SPEED)
    object_ship_jason.speed = PLAYER_MAX_SPEED;

// Min speed cap
if(object_ship_jason.speed <= PLAYER_MIN_SPEED)
    object_ship_jason.speed = PLAYER_MIN_SPEED;

